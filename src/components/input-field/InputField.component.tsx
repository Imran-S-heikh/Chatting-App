import React, { ReactElement } from 'react'

interface Props {

}

function InputField({ }: Props): ReactElement {
    return (
        <div>
            Input Field
        </div>
    )
}

export default InputField
